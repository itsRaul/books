<view class="classic-container" hidden="{{hidden}}">
    <image src="{{img}}" class="classic-img"/>
    <image src="./images/movie@tag.png" class="tap"/>
    <text class="content">{{content}}</text>
</view>
